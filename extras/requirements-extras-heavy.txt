# =============================================================================
# Hermes 离线增强包 —— 「重物」可选组
# =============================================================================
#
# 这几个包本身都合法、也都有 aarch64 轮子，只是体积大（合计解压后数百 MB）：
#   · opencv-python-headless ~35 MB 轮子 + numpy
#   · scipy                 ~35 MB
#   · scikit-image          ~13 MB（依赖 scipy）
#   · jupyterlab 全家桶      ~60 个包
#   · ipython / black       ——
#
# 默认包含。构建时加 `-f with_heavy=false` 可以整组剔除，
# 用来在网络很差、只想先拿到"办公底座"的时候快速出一版。
# =============================================================================

opencv-python-headless
scipy
scikit-image
seaborn
ipython
jupyterlab
black
